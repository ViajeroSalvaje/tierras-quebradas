<div style="height:100%;width:100%;display:flex;flex-direction:column;">
  <div class="item-body" style="flex:1;overflow-y:auto;padding:8px 12px;display:flex;flex-direction:column;gap:10px;">

    <h1><input type="text" name="name" value="{{item.name}}" placeholder="{{localize "TQ.Placeholders.NombreEntorno"}}" /></h1>

    <div class="tq-field-group full">
      <label>{{localize "TQ.General.Descripcion"}}</label>
      <textarea name="system.descripcion" rows="5">{{system.descripcion}}</textarea>
    </div>

    <div class="tq-field-group full">
      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:4px;">
        <label>{{localize "TQ.General.ModificadoresHabilidades"}}</label>
        <a class="ent-hab-add" style="font-size:11px;color:var(--tq-teal);cursor:pointer;"><i class="fas fa-plus"></i> {{localize "TQ.Botones.Anadir"}}</a>
      </div>
      <table style="width:100%;font-size:12px;">
        <thead><tr><th style="text-align:left;">{{localize "TQ.General.Habilidad"}}</th><th style="width:60px;text-align:center;">{{localize "TQ.General.Bonus"}}</th><th style="width:30px;"></th></tr></thead>
        <tbody>
          {{#each habilidades as |h|}}
          <tr>
            <td>
              <select class="ent-hab-clave" data-index="{{h.index}}" data-field="clave" style="width:100%;">
                <option value="">— Seleccionar —</option>
                {{#each h.opciones as |op|}}<option value="{{op.clave}}" {{#if op.selected}}selected{{/if}}>{{op.label}}</option>{{/each}}
              </select>
            </td>
            <td><input type="number" class="ent-hab-bonus" data-index="{{h.index}}" data-field="bonus" value="{{h.bonus}}" style="width:55px;text-align:center;" /></td>
            <td><a class="ent-hab-del" data-index="{{h.index}}" style="color:var(--tq-red-dmg);cursor:pointer;"><i class="fas fa-trash"></i></a></td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>

  </div>
</div>
